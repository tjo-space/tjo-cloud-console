---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: buckets.s3.tjo.cloud
spec:
  group: s3.tjo.cloud
  names:
    categories: []
    kind: Bucket
    plural: buckets
    shortNames:
    - buc
    singular: bucket
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BucketSpec via `CustomResource`
        properties:
          spec:
            description: Represents a bucket in s3.tjo.cloud.
            properties:
              name:
                maxLength: 63
                minLength: 3
                pattern: '[a-z0-9.-]+'
                type: string
            required:
            - name
            type: object
          status:
            nullable: true
            properties:
              created:
                type: boolean
            required:
            - created
            type: object
        required:
        - spec
        title: Bucket
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: tokens.s3.tjo.cloud
spec:
  group: s3.tjo.cloud
  names:
    categories: []
    kind: Token
    plural: tokens
    shortNames:
    - tok
    singular: token
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for TokenSpec via `CustomResource`
        properties:
          spec:
            description: Represents a token to access bucket in s3.tjo.cloud.
            properties:
              bucket_ref:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              owner:
                type: boolean
              reader:
                type: boolean
              writer:
                type: boolean
            required:
            - bucket_ref
            - owner
            - reader
            - writer
            type: object
          status:
            nullable: true
            properties:
              created:
                type: boolean
            required:
            - created
            type: object
        required:
        - spec
        title: Token
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: databases.postgresql.tjo.cloud
spec:
  group: postgresql.tjo.cloud
  names:
    categories: []
    kind: Database
    plural: databases
    shortNames:
    - dat
    singular: database
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for DatabaseSpec via `CustomResource`
        properties:
          spec:
            description: Database on the postgresql.tjo.cloud database platform
            properties:
              connection_limit:
                format: int32
                type: integer
              owner_ref:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              server:
                type: string
            required:
            - connection_limit
            - owner_ref
            - server
            type: object
          status:
            nullable: true
            properties:
              created:
                type: boolean
              name:
                type: string
            required:
            - created
            - name
            type: object
        required:
        - spec
        title: Database
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: users.postgresql.tjo.cloud
spec:
  group: postgresql.tjo.cloud
  names:
    categories: []
    kind: User
    plural: users
    shortNames:
    - user
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for UserSpec via `CustomResource`
        properties:
          spec:
            description: User on the postgresql.tjo.cloud database platform
            properties:
              connection_limit:
                format: int32
                type: integer
              password_secret_name:
                description: Name of the secret that will be created and contain the generated password.
                type: string
              server:
                type: string
            required:
            - connection_limit
            - password_secret_name
            - server
            type: object
          status:
            nullable: true
            properties:
              created:
                type: boolean
              name:
                type: string
            required:
            - created
            - name
            type: object
        required:
        - spec
        title: User
        type: object
    served: true
    storage: true
    subresources:
      status: {}
